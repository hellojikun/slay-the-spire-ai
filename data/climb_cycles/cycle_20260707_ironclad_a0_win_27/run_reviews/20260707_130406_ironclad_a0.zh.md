# 单局复盘：20260707_130406_ironclad_a0

## 结论

本局为 Ironclad A0 live MCP 爬塔，Boss swap 到 `Fusion Hammer`，成功击败 Slime Boss，并在 F25 Act2 双 Cultist 死亡。分类为 `clean_trainable/completed_clean`，失败归因为 `combat_planning`。这是 `_27` 中最有价值的一局：Act1 通过，Act2 推进到 F25，暴露了无药水、低血量、连续普通战的真实短板。

## 受启发式/搜索影响的行为

- 路线和战斗仍由旧 route risk、rest policy、potion tempo、direct lethal 和 one-turn search 控制。
- F15 选择休息而不是 smith 是正确的启发式行为；同时 `Fusion Hammer` 禁止 smith，所以本局天然更依赖卡牌奖励和战斗执行。
- F21 双敌开局连续使用三瓶药水，配合 `Toy Ornithopter` 把 HP 从 55 拉到 70，这是 potion tempo 的强正样本。
- F24 Snake Plant 和 F25 双 Cultist 暴露了短视问题：策略能局部杀怪，但无药水后对 Act2 连续战斗的未来损失估计不足。
- F21 曾出现敌人低血、Whirlwind 可用但结束回合的可疑点，后续应检查 X-cost/0 能量时的价值计算。

## 学习/模型影响

- F4 选择 `Spot Weakness` 时记录了 `heuristic_with_memory_model_score`：启发式基础分较高，学习记忆/模型给了负修正，但未推翻选择。
- Act1 Boss 奖励选择 `Bludgeon` 也记录了 card reward assist；模型仍是 assist，`runtime_authority=false`。
- 本局训练后 card model 使用 18 个本地例子更新，external blend 也训练完成；但 shadow models 和 combat value 因 source-quality 限制未在本轮重新训练。

## 外部数据影响

- 外部数据通过 `external_prior` 与 `card_value_model_external_prior_blend` 影响候选训练，不控制本局 live MCP。
- 这局中的 `Spot Weakness`、`Bludgeon`、`Uppercut`、`Hemokinesis` 等选择可用于后续对比外部 prior 与本地 A0 结果：外部倾向只能作为低权重候选，不能替代本地胜负证据。

## 后续影响

- 本局支持推进到更高质量的 A0 验证批，但不能宣称终局胜利。
- 主要改进方向是 Act2 combat planning：多敌优先级、Ritual 滚雪球、药水耗尽后的路线风险、以及未来 2-4 回合血量模型。
- 本局证明模型可以继续扩大接管范围，但下一步应先让模型在离线/assist 中证明能改善 Act2 行为，再进入小流量 live authority。
