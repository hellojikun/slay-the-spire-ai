# 单局复盘：20260707_120608_ironclad_a0

## 结论

本局 F7 早死，失败归因为 `route_risk`。不是 `max-step` 限制导致，也不是外部模型误导；它是旧执行层路线/战斗搜索在早期资源链上的失败样本。

## 受启发式/搜索影响的行为

- 路线、事件、休息、药水和出牌仍由 policy、route risk、potion tempo、direct lethal 与 one-turn search 控制。
- F7 Gremlin Gang 前，牌组为 Snecko Eye、Warcry、Seeing Red 等偏不稳定组合，路线继续进入高压遭遇。
- F7 T3 面对 29 incoming 时使用 Swift Potion 属于 emergency tempo 启发式。
- F7 T4 的 `Bash/Defend/Strike/Strike` 搜索线减少了当回合损失，但无法修复前面路线和牌组质量带来的血量压力。

## 学习/模型影响

- 本局没有 `runtime_authority=true`，模型没有接管 live MCP。
- combat value 只作为 `search.combat_value_shadow` 记录，不改变动作。
- 本局结束后进入 cycle 训练，贡献早期 route-risk、Snecko Eye 低稳定性、Warcry/Seeing Red 奖励选择等负样本。

## 外部数据影响

- `mat1g3r_200_rotating_ironclad` A20 prior 只参与赛后 external prior 和 external blend 训练。
- 外部 prior 的 `runtime_authority=false`、`does_not_control_live_mcp=true`，不进入 A0 gate、pristine 判定或 learned memory。
- 本局 live 行为不能归因给外部数据。

## 后续影响

下一步应继续关注 `improve_route_and_early_act1_survival`：尤其是 F6-F8 低质量牌组进入高压遭遇的风险。不要为这局单独加硬编码例外，优先让 route/future-loss 模型学习这类失败。
