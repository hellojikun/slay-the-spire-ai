# 单局复盘：20260707_140626_ironclad_a0

日期：2026-07-07  
来源日志：`runs/ai_runs_climb_cycle_ironclad_a0_win_28/20260707_140626_ironclad_a0.jsonl`  
目标：Ironclad A0 终局胜利

## 本局结果

本局是 `_28` 最好的一局，`clean_trainable/completed_clean`，validation grade 为 `usable_with_recoveries`。Act1 Slime Boss 已击败，进入 Act2 后最终在 F27 死于 Chosen + Cultist。失败归因为 `route_risk`。

最终回合中，Chosen 仅剩约 4 HP，但玩家 HP 约 15/56，面对 21 incoming 且无足够格挡，结束回合后死亡。之后 MCP 返回 null，系统用 synthetic terminal 记录终局，terminal recovery 成功。

## 启发式/搜索影响

本局体现了搜索的上限和路线启发式的问题：

- Act1 通过 Slime Boss，说明前期战斗、升级和奖励选择仍足以完成 A0 Act1。
- 牌组包含 Shockwave+、Whirlwind+、Carnage、Reaper、Burning Pact、Thunderclap、Immolate、Battle Trance，后续还经历 Bites 事件。
- F19 路线选择是关键错误：`M` 选项分数更高，但后续有强制精英风险；`?` 选项分数较低但更接近休息/商店。系统选择 `M`，后续被推入更危险的 Act2 路线。
- F21 Bites 事件把最大 HP 从 80 降到 56，换来 5 张 Bite。Bite 确实提供续航，但不应被解释为可以承受强制精英和连续高伤战斗。
- F25 营火时 HP 约 12/56，因为 Coffee Dripper 不能休息，系统选择 smith 并记录近似“HP safe”的理由。这是 rest policy 对 Coffee Dripper 的解释缺陷。
- F27 战斗使用 Blessing of the Forge、先杀 Cultist，说明战斗搜索仍在尝试短期最优；但路线和血量已经把局面推到一回合差距的失败。

## 学习/模型影响

本局模型没有接管 live 执行层。`model_authority=assist`，`runtime_authority=false`。战斗中的 search、路线选择、营火 smith、药水使用都应归为启发式/搜索行为，而不是神经网络决策。

本局对学习最有价值的是路线风险标签：高分路线如果导致未来强制精英、缺少恢复、低血量连续战，应被模型或评分系统强力惩罚。F25 Coffee Dripper 下的 smith 也应进入后续复盘标签，作为“不能休息时不要把营火当恢复点”的训练/规则边界。

## 外部数据影响

本轮使用的外部 prior 仍只参与训练产物，不控制本局行为。external prior/blend 训练成功，但 `runtime_authority=false`、`runtime_default_enabled=false`、`does_not_control_live_mcp=true`。

因此，Bites、路线 M 选择、F25 smith、F27 战斗线都不能归因于外部数据。外部数据对后续可能帮助卡牌先验，但不能修复这类路线/营火执行问题，除非未来建立经过审计的 route/action trace 训练集。

## 执行完整性

本局有 synthetic terminal，原因是终局附近 MCP null。恢复成功后仍被归类为 usable with recoveries。它适合写入复盘、card model 和 learned memory；如果要训练 pristine combat value，需要谨慎排除或等待更干净样本。

`max-step=3000` 未影响本局；本局是实际死亡，不是超步数截断。

## 对后续影响

本局给下一轮最明确的改进方向：

1. 路线模型必须学会“低即时收益但保命”的价值，尤其在 Act2 有更安全路线时，不应被高分 `M` 节点诱导进强制精英链。
2. Coffee Dripper 下营火不能被当作恢复资源；smith 需要根据实际 HP、后续节点和无法休息状态重新评估。
3. Bite/Reaper 这类续航牌只能降低风险，不能抵消错误路线带来的强制精英和连续战斗风险。
4. 下一步模型接管应优先做 route-risk veto/shadow audit，而不是直接接管战斗点击。
