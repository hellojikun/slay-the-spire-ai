# 单局复盘：20260707_091949_ironclad_a0

## 本局概况

- 源日志：`runs\ai_runs_climb_cycle_ironclad_a0_win_21\20260707_091949_ironclad_a0.jsonl`
- 分类：`clean_trainable` / `completed_clean`
- 角色/进阶：`IRONCLAD` A0
- 最终记录楼层：F16 Hexaghost；胜利：`false`
- validation：`usable_with_recoveries`，recovered action 1，failed action 0
- 失败归因：`combat_planning`
- Act1 Boss：reached=True，cleared=False；F16 Hexaghost，entry_hp=69，potion_use_steps=[201]

## 启发式与搜索的影响

本局 live 控制仍由现有 policy、route risk、rest/shop/potion 评分和 one-turn search 驱动。路线与战斗推进能稳定到 F16，但 Boss 战后段没有把 Hexaghost 长战未来血线转成胜势规划，最终在 Boss 剩余血量不低时耗尽生命。

本局出现 1 次 recovered action race 和 synthetic terminal，因此 Act1 Boss 前缀不是 pristine。它仍可作为 clean_trainable 的失败样本，但不能当作 pristine boss clear 或稳定终局证据。

## 学习与模型的影响

本局 `model_authority=assist`，但没有战斗执行层 `runtime_authority=true`。卡牌奖励可能带有有限 assist 元数据；`search.combat_value_shadow` 只记录神经网络对 one-turn search 的影子价值判断，`runtime_authority=false`，没有改变 `play_card`。

本局死亡不能归因于神经网络接管。它主要暴露的是现有搜索/执行层对 Hexagost 长战未来损血、输出窗口和防御曲线估计不足。

## 训练影响

本局可进入默认 clean_trainable 复盘和有限学习链路，但因为 `validation_grade=usable_with_recoveries`，不应进入 `source_quality=pristine` 的 shadow/combat value 训练。若后续用作 combat label，需要保留 recovered action 与 synthetic terminal 标记，避免把终局恢复噪声误当作策略质量。

外部数据未配置，未进入本局 runtime、gate、learned_memory、pristine 或 combat value 训练。

## 对后续爬塔的影响

下一轮不应继续添加单点启发式例外。更好的方向是把 Hexaghost 后段回合转成 future-loss/combat value 对照样本，观察 shadow value 是否能解释“短期减伤但长期仍输”的局面。
