# 单局复盘：20260707_093056_ironclad_a0

## 本局概况

- 源日志：`runs\ai_runs_climb_cycle_ironclad_a0_win_21\20260707_093056_ironclad_a0.jsonl`
- 分类：`diagnostic_excluded` / `no_terminal_outcome`
- 角色/进阶：`IRONCLAD` A0
- 最终记录楼层：F17 GRID；胜利：`null`
- validation：`diagnostic`，recovered action 65，failed action 0
- 失败归因：`diagnostic_incomplete`
- Act1 Boss：F16 TheGuardian cleared=True，entry_hp=58，clear_step=249，last_hp=22，prefix_pristine=True

## 启发式与搜索的影响

本局前缀有价值：策略成功清除 Guardian，并以 22/80 进入 Act2 前的 Boss relic 结算。此处 live 行为仍来自现有 policy、药水策略与 one-turn search；Guardian clear 本身不是神经网络接管。

中止原因是 F17 选择 Astrolabe/星盘后进入三张牌 GRID，多选只选了 Offering+ 后，策略反复尝试 `confirm`，但 MCP 可用命令只有 `choose`。从 step 253 起连续出现 `Preflight unavailable action(s): ['confirm']; available commands: ['choose']`，导致 64 次 unavailable action recovery。更早一步还出现 CHEST `proceed` 与 `choose` 的 recoverable mismatch。

## 学习与模型的影响

本局没有有效训练。`model_authority=assist` 未接管战斗；`search.combat_value_shadow` 只作为观察元数据。Astrolabe 卡住与模型无关，是 GRID 执行层把“多选未完成”误判为“已有选择可以确认”，并且旧选择逻辑会把高价值 Offering+ 拿去变掉。

## 训练影响

整局是 `diagnostic_excluded/no_terminal_outcome`，不能进入 learned_memory、card model、shadow model 或 neural combat value 训练。可以保留两类证据：一是 Guardian prefix_pristine clear 片段；二是 Astrolabe GRID 多选执行层缺陷。

外部数据未配置，没有影响本局动作。外部来源仍只能作为 reference/scratch prior，不计入 A0 gate 或 pristine。

## 对后续爬塔的影响

已修复 `slay_ai.policy_rest`：多选 GRID 会根据 `num_cards` 继续选择未选卡，星盘这类多选变牌会选择低价值卡并跳过已选卡；单选已选中的 bottle/类似场景仍可确认。现场只读校验显示当前 F17 GRID 的下一步已从错误的 `confirm` 变为 `choose`。该修复属于执行层状态机，不是新增爬塔启发式，也不会把本诊断局转成训练样本。
