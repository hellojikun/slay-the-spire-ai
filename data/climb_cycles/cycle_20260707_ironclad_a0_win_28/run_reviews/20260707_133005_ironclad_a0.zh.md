# 单局复盘：20260707_133005_ironclad_a0

日期：2026-07-07  
来源日志：`runs/ai_runs_climb_cycle_ironclad_a0_win_28/20260707_133005_ironclad_a0.jsonl`  
目标：Ironclad A0 终局胜利

## 本局结果

本局为 `clean_trainable/completed_clean`，validation grade 为 `usable_with_recoveries`。Act1 The Guardian 已击败，随后进入 Act2；最终在 F24 死于 Book of Stabbing。失败归因为 `combat_planning`。

关键路线：Rampage、Cleave、Double Tap、Shrug It Off、True Grit、Demon Form 等进入牌组；Boss relic 选择 Coffee Dripper。Coffee Dripper 提供能量，但从此不能通过营火回血，Act2 资源缓冲明显变窄。

## 启发式/搜索影响

本局大部分 live 行为由旧策略和 one-turn search 主导：

- Act1 能击败 The Guardian，说明基础路线、卡牌奖励和战斗搜索仍能稳定通过 A0 Act1。
- Book of Stabbing 战斗中，搜索多次尝试在当前回合内兼顾输出、防御或斩杀，但它只看短期局面，没能解决此前路线和资源缓冲造成的长期劣势。
- F24 Book of Stabbing 中 HP 逐步从 63 降到 56、52、38、33、12，最终死亡；这更像战斗规划和资源使用时机失败，而不是模型接管导致。
- 本局有一次可恢复 action race：select_cards 不可用时改选可用动作，恢复成功，属于执行层保护，不是策略优势。

## 学习/模型影响

本局 live 执行层没有被神经网络模型接管。`model_authority=assist`，`runtime_authority=false`。

卡牌奖励里能看到 learned/model delta 记录，但最终选择仍主要受启发式与当前策略控制。例如 Cleave 这类 AOE/前期战力卡的选择更接近启发式需要，而不是外部 prior 或神经网络独立决策。本局数据会进入 card model 和 learned memory 的本地学习闭环，但不会证明模型已经能控制战斗。

## 外部数据影响

本轮 cycle 使用了外部 manifest：`data/external/sts_runs/mat1g3r_200_rotating_ironclad/external_manifest.json`。该数据只在训练阶段形成 external prior/blend，权限为 `runtime_authority=false`、`does_not_control_live_mcp=true`。

因此，本局 Rampage/Cleave/Double Tap 等 live 行为不能写成由外部数据直接控制。外部数据对后续的影响是提供低权重卡牌先验，而非替代本地 MCP 日志。

## 执行完整性

本局没有 infra-blocked。action race 被恢复，日志仍可用于复盘和部分学习；但高洁净度 shadow/combat value 训练仍应遵守 source-quality 限制。

## 对后续影响

本局提示：Act2 精英战不能只依赖 one-turn search。Book of Stabbing 需要更早的路径/药水/血量缓冲判断；Coffee Dripper 局尤其要避免在没有恢复手段时把路线推入强制高伤战斗。后续训练应把这类“短期能打、长期资源崩盘”的局面转成 route-risk 和 combat-value 的训练目标。
