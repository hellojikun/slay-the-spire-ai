# 单局复盘：20260707_092541_ironclad_a0

## 本局概况

- 源日志：`runs\ai_runs_climb_cycle_ironclad_a0_win_21\20260707_092541_ironclad_a0.jsonl`
- 分类：`clean_trainable` / `completed_clean`
- 角色/进阶：`IRONCLAD` A0
- 最终记录楼层：F16 The Guardian；胜利：`false`
- validation：`pristine`，recovered action 0，failed action 0
- 失败归因：`potion_planning`
- Act1 Boss：reached=True，cleared=False；entry_hp=63，entry_potion_count=0，last_turn=13

## 启发式与搜索的影响

本局是 `_21` 中最干净的一盘。路线、休息、卡牌奖励、Boss 前资源使用和战斗出牌都来自现有启发式与 one-turn search。它 63/80 进入 Guardian，但 Boss 前没有保留 tempo potion，Guardian 战中 T10 出现 `missed_single_card_search` 审计排除项，最终 T13 死亡。

这盘说明当前执行层能产生 pristine 失败样本，但 potion tempo 和 Guardian 长战防御/输出顺序仍不足。离线诊断给出的 `fix_combat_search_priority` 与 `missed_single_card_search` 应进入审计和回放，而不是直接扩大模型权限。

## 学习与模型的影响

本局 `model_authority=assist` 没有扩大到战斗执行层；没有 `runtime_authority=true` 的 `play_card` 接管。`search.combat_value_shadow` 若存在，也只是本地模型对搜索行的 shadow metadata。

因此本局中 Guardian T2/T10/T13 的战斗行为仍应归因于启发式、药水规划和搜索优先级，而不是神经网络。模型观察已记录、未用于本轮训练、未接管执行层。

## 训练影响

本局是 pristine clean_trainable，可以作为本地 MCP 训练和审计的重要失败样本。combat search label 中有 1 条 `missed_single_card_search` 被排除，不能强行作为正确标签训练；应优先跑 replay audit，确认当前 policy 是否已经匹配实际或标签。

外部数据未配置，未进入 runtime、gate、learned_memory、pristine 统计或本轮神经网络训练。

## 对后续爬塔的影响

下一轮应继续 A0，并重点观察 Guardian/Hexaghost 的 potion tempo 与单卡搜索优先级。如果仍在 Boss 战中死亡，先用本地 pristine 样本训练或评估 combat value / combat_search shadow，而不是把外部 run history 当成逐动作控制标签。
