# 单局复盘：20260707_121229_ironclad_a0

## 本局概况

- 源日志：`runs\ai_runs_climb_cycle_ironclad_a0_win_26\20260707_121229_ironclad_a0.jsonl`
- 分类：`clean_trainable` / `completed_clean`
- 角色/进阶：`IRONCLAD` A0
- 最终楼层：`16`；胜利：`False`
- 失败归因：`deck_quality`
- 验证等级：`usable_with_recoveries` flags=['synthetic_terminal', 'synthetic_after_mcp_null', 'synthetic_terminal_state']
- 动作恢复：total=0 recovered=0 unrecovered=0
- 第一幕 Boss：reached=True cleared=False prefix_pristine=False blockers=['synthetic_terminal_state', 'synthetic_terminal', 'synthetic_after_mcp_null']

## 启发式与搜索的影响

- 本局 live 控制仍由现有策略、执行保护和 one-turn combat search 驱动。
- 关键启发式/搜索决策计数：search/direct-lethal=92，route=16。
- 这些记录代表控制器或搜索器的实时选择，不等同于训练后模型的直接产出。
- Step 4 F0：Route to M at x=0 score 62.0.
- Step 7 F1：One-turn search: sequence ['Bash', 'Strike_R'] loss 14->5, damage=10, retaliation_damage=0, kills=1, attacks_removed=9, avoided_lethal=False, score=75.0.
- Step 10 F1：One-turn search: sequence ['Bash', 'Strike_R'] loss 5->0, damage=8, retaliation_damage=0, kills=1, attacks_removed=5, avoided_lethal=False, score=71.5.
- Step 11 F1：Play 打击 for direct lethal.
- Step 13 F1：Route to ? at x=0 score 38.0.
- Step 18 F2：Route to ? at x=0 score 47.0 (map risk +9.0).
- Step 21 F3：Route to M at x=0 score 71.0 (map risk +9.0).
- Step 22 F4：One-turn search: sequence ['Bash', 'Strike_R'] loss 3->0, damage=11, retaliation_damage=0, kills=1, attacks_removed=3, avoided_lethal=False, score=62.8.
- Step 28 F4：One-turn search: sequence ['Bash', 'Strike_R'] loss 8->0, damage=16, retaliation_damage=0, kills=1, attacks_removed=8, avoided_lethal=False, score=94.0.
- Step 32 F4：Pick 耸肩无视; reward score 67.6.
- Step 34 F4：Route to ? at x=0 score -14.0 (map risk -52.0).
- Step 37 F5：Route to R at x=0 score 63.0 (map risk +18.0).
- Step 38 F6：HP is safe; smith.
- Step 41 F6：Rest site complete.
- Step 42 F6：Route to ? at x=0 score -32.0 (map risk -70.0).
- Step 43 F7：Enter shop.
- Step 44 F7：Shop buy 力量药水 for 49 gold; score 60.0.
- Step 45 F7：No high-confidence shop purchase; leave.

## 学习与模型的影响

- 本局记录到的模型权限档位：`assist`。
- 本局有模型权限元数据，但没有发生 runtime_authority=true 的接管；模型仍处于影子/辅助观察。
- 本局记录到的卡牌奖励选择，可用于卡牌价值学习：
- Step 32 F4：`Shrug It Off`
- Step 106 F10：`Thunderclap`
- Step 124 F11：`Second Wind`
- Step 153 F12：`Ghostly Armor`
- Step 174 F13：`Spot Weakness`
- 本轮 shadow 训练状态：`trained` rows=80。
- 本轮卡牌模型训练状态：`trained` examples=15。
- 本轮神经网络 combat value shadow 状态：`trained` examples=41；runtime_authority=False。
- 本轮外部数据 prior 状态：`trained` examples=889；runtime_authority=False。
- 本轮外部融合候选状态：`trained` deltas=151；runtime_authority=False。

## 训练影响

- `clean_trainable`：可进入 `learn` 和 `train_card_model --manifest`。
- 本局验证等级为 `usable_with_recoveries`；默认 shadow 训练只接收 `pristine` 证据。
- 本局可用于复盘和卡牌学习，但不应进入默认 pristine shadow 训练。

## 对后续爬塔的影响

- 批次建议：`improve_route_and_early_act1_survival`。
- 不因为单局异常直接新增启发式例外；先看 manifest、diagnosis、gate 和重复证据。
- 如果本局是 infra-blocked 或 diagnostic，保留为执行层证据，不进入默认训练。
- 如果模型接管造成错误选择，下一轮先降回 `shadow` 或收紧接管阈值，再继续收集 A0 终局证据。
